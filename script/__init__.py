"""Package initializer for the `script` package.

This file makes the `script` directory importable as a package so
you can run modules with `python -m script.module` and use
relative imports inside the package.
"""

__all__ = []
